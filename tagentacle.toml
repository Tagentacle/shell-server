[package]
name = "shell_server"
version = "0.1.0"
description = "MCP Server exposing exec_command for container shell execution"
authors = ["Tagentacle Contributors"]
type = "executable"

[entry_points]
node = "shell_server:main"

[dependencies]
python = ["tagentacle-py-mcp"]
system = ["docker"]
