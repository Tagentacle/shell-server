[package]
name = "shell_server"
version = "0.2.0"
description = "MCP Server providing exec_command (TACL space / container / local)"
authors = ["Tagentacle Contributors"]
type = "executable"

[entry_points]
node = "shell_server:main"

[dependencies]
python = ["tagentacle-py-mcp"]
# docker is optional — only needed when TARGET_CONTAINER is set
# system = ["docker"]
